<p id="notice"><%= notice %></p>

<h1>Listing Add Type To Users</h1>

<table>
  <thead>
    <tr>
      <th>Type</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @add_type_to_users.each do |add_type_to_user| %>
      <tr>
        <td><%= add_type_to_user.type %></td>
        <td><%= link_to 'Show', add_type_to_user %></td>
        <td><%= link_to 'Edit', edit_add_type_to_user_path(add_type_to_user) %></td>
        <td><%= link_to 'Destroy', add_type_to_user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Add type to user', new_add_type_to_user_path %>
